---
- name: Deploy Streaming Stack
  hosts: streaming.homelab
  become: true

  pre_tasks:
    - name: Ensure Docker is running
      ansible.builtin.service:
        name: docker
        state: started
        enabled: true

  roles:
    - gpu_passthrough
    - nfs_media
    - cloudflare_tunnel
    - docker_compose
