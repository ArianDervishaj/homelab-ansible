---
- name: Deploy Media/Books Stack
  hosts: library.homelab
  become: true

  pre_tasks:
    - name: Ensure Docker is running
      ansible.builtin.service:
        name: docker
        state: started
        enabled: true

  roles:
    - nfs_media
    - docker_compose
