---
# Not included by main.yml. Only runs when called with tasks_from: restore

- name: Require explicit confirmation
  ansible.builtin.assert:
    that: restic_backup_restore_confirm | bool
    fail_msg: "Restore not confirmed. Re-run with -e restic_backup_restore_confirm=true"

- name: Select jobs to restore
  ansible.builtin.set_fact:
    __restic_backup_restore_jobs: >-
      {{ restic_backup_jobs if (restic_backup_restore_jobs | length == 0)
         else (restic_backup_jobs | selectattr('name', 'in', restic_backup_restore_jobs) | list) }}
    __restic_backup_in_place: "{{ restic_backup_restore_target == '/' }}"
    __restic_backup_restore_ts: "{{ ansible_facts['date_time']['epoch'] }}"
    __restic_backup_restore_snaps: {}

- name: Skip host with nothing to restore
  when: __restic_backup_restore_jobs | length == 0
  block:
    - name: Report skip
      ansible.builtin.debug:
        msg: "No matching backup jobs on {{ inventory_hostname }}, skipping."

    - name: End host
      ansible.builtin.meta: end_host

# Also fails early on wrong password or missing repo, BEFORE stopping anything
- name: List snapshots of each repo
  ansible.builtin.command: >-
    {{ restic_backup_install_path }} -r {{ restic_backup_base }}/{{ inventory_hostname }}/{{ item.name }}
    snapshots --json
  environment:
    RESTIC_PASSWORD: "{{ restic_backup_password }}"
  loop: "{{ __restic_backup_restore_jobs }}"
  loop_control:
    label: "{{ item.name }}"
  changed_when: false
  register: __restic_backup_snapshot_list

# Picks one snapshot per job:
#   restic_backup_restore_before set -> newest snapshot strictly before that date
#   otherwise                        -> restic_backup_restore_snapshot ("latest" or an ID)
- name: Pick snapshot for each job
  ansible.builtin.set_fact:
    __restic_backup_restore_snaps: "{{ __restic_backup_restore_snaps | combine({item.item.name: _picked}) }}"
  vars:
    _all: "{{ item.stdout | from_json }}"
    _candidates: >-
      {{ (_all | selectattr('time', '<', restic_backup_restore_before) | list)
           if (restic_backup_restore_before | length > 0)
         else (_all if restic_backup_restore_snapshot == 'latest'
         else (_all | selectattr('id', 'match', restic_backup_restore_snapshot) | list)) }}
    _picked: "{{ (_candidates | last) if (_candidates | length > 0) else {} }}"
  loop: "{{ __restic_backup_snapshot_list.results }}"
  loop_control:
    label: "{{ item.item.name }}"

- name: Fail if a job has no matching snapshot
  ansible.builtin.assert:
    that: __restic_backup_restore_snaps[item.name].short_id is defined
    fail_msg: >-
      No snapshot found for {{ item.name }}
      ({{ 'before ' ~ restic_backup_restore_before if restic_backup_restore_before | length > 0
          else restic_backup_restore_snapshot }}). Nothing was stopped or changed.
    quiet: true
  loop: "{{ __restic_backup_restore_jobs }}"
  loop_control:
    label: "{{ item.name }}"

- name: Show snapshot to be restored
  ansible.builtin.debug:
    msg: >-
      {{ item.name }}: {{ __restic_backup_restore_snaps[item.name].short_id }}
      from {{ __restic_backup_restore_snaps[item.name].time }}
  loop: "{{ __restic_backup_restore_jobs }}"
  loop_control:
    label: "{{ item.name }}"

- name: Stop stack
  ansible.builtin.systemd:
    name: "{{ docker_compose_stack_name }}"
    state: stopped
  when: __restic_backup_in_place

# restic restore doesn't delete extra files, so move current data aside.
# This also gives you a rollback copy.
- name: Move current data aside
  ansible.builtin.command: "mv {{ item.1 }} {{ item.1 }}.pre-restore-{{ __restic_backup_restore_ts }}"
  args:
    removes: "{{ item.1 }}"
  loop: "{{ __restic_backup_restore_jobs | subelements('paths') }}"
  loop_control:
    label: "{{ item.1 }}"
  when: __restic_backup_in_place

- name: Restore snapshot
  ansible.builtin.command: >-
    {{ restic_backup_install_path }} -r {{ restic_backup_base }}/{{ inventory_hostname }}/{{ item.name }}
    restore {{ __restic_backup_restore_snaps[item.name].short_id }} --target {{ restic_backup_restore_target }}
  environment:
    RESTIC_PASSWORD: "{{ restic_backup_password }}"
  loop: "{{ __restic_backup_restore_jobs }}"
  loop_control:
    label: "{{ item.name }}"

- name: Start stack
  ansible.builtin.systemd:
    name: "{{ docker_compose_stack_name }}"
    state: started
  when: __restic_backup_in_place

- name: Summary
  ansible.builtin.debug:
    msg: >-
      Restored {{ __restic_backup_restore_jobs | map(attribute='name') | join(', ') }}
      to {{ restic_backup_restore_target }}.
      {{ 'Old data kept as *.pre-restore-' ~ __restic_backup_restore_ts
         if __restic_backup_in_place else 'Test restore, services untouched.' }}
